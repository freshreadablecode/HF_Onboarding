# Python Setup (after platform specefic setup)

## Making sure Python is installed

```bash
python3 --version
```

## Installing Required Packages

```bash
python3 -m pip install --upgrade pip
pip3 install -r python_requirements.txt
```
